# Root Directory

+ This directory represents the root directory in the linux filesystem.
+ Files that are in the root directory are linked here for tracking.
+ For example: `/etc/fstab` will be hard linked to `./etc/fstab`.

---
